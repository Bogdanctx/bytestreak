info:
  name: /friends/pending-connections
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/friends/pending-connections
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​